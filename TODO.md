# TODO - Sửa code theo package hiện tại

- [ ] Xác định lỗi Flutter đang chạy sai root (flutter run từ thư mục chứa lib/main.dart không tồn tại)
- [ ] Hướng dẫn chạy đúng thư mục Flutter: `cd frontend` rồi `flutter run`
- [ ] Chạy `flutter pub get` trong `frontend`
- [ ] Chạy `flutter analyze` và sửa các lỗi dependency/API nếu phát sinh
- [ ] (Nếu cần) kiểm tra backend `pom.xml` và chạy `mvn test`

